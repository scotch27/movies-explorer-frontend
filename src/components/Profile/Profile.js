// MoviesCard — компонент одной карточки фильма.

import './Profile.css';

function Profile() {
    return (
        <div className="Profile">
            <h1>Profile</h1>
        </div>
    );
}

export default Profile;
